{
  "$schema": "https://schema.tauri.app/config/2/capability.json",
  "identifier": "chrome-probe",
  "description": "Chrome DevTools discovery: Rust command probe_chrome_ports uses reqwest only to http://127.0.0.1:{port}/json/version and /json (default port sweep 9222–9230). No outbound WAN; loopback only.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
