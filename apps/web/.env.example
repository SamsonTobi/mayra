# Web deployment mode — set to "web" for the standalone web UI, "desktop" (or unset) for Tauri
NEXT_PUBLIC_MAYRA_MODE=web

# Cloud orchestrator URL — the Modal deployment URL
NEXT_PUBLIC_CLOUD_ORCHESTRATOR_URL=https://your-deployment--mayra-orchestrator-mayra-web.modal.run
