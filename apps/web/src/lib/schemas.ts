/** Re-export contract shapes for UI (spec §3.3 `schemas.ts`). */
export type {
  Action,
  ActionLogMessage,
  ApprovalRequestMessage,
  ChatMessage,
  SseEventPayload,
  SystemStatusMessage,
} from "@mayra/contracts";
