"use client";

export function RetentionSettings() {
  return (
    <section className="card">
      <h2>Retention</h2>
      <p className="muted">
        30-day window and local screenshot cleanup (spec §C.6) — orchestrator-owned; UI shows
        copy only.
      </p>
    </section>
  );
}
